name: tinderclone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  faker: ^2.2.0
  flutter:
    sdk: flutter
  flutter_card_swiper: ^7.2.0
  gap: ^3.0.1
  go_router: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
